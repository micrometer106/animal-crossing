{
  "name": "animal-crossing",
  "homepage": "https://micrometer106.github.io/animal-crossing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "predeploy": "yarn run build",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && gh-pages -d out -t true",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "next": "11.1.3",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
